info:
  name: Delete Non-Existing Artifact
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/artifact/9999"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 404 for non-existing artifact", function() {
          expect(res.status).to.equal(404);
        });
