info:
  name: 23 Delete Referenced Record A
  type: http
  seq: 21

http:
  method: DELETE
  url: "{{baseUrl}}/api/record/{{labeledRecordIdA}}"
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
