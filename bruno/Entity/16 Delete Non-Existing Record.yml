info:
  name: 17 Delete Non-Existing Entity
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{baseUrl}}/api/record/9999"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 404 for non-existing record", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
