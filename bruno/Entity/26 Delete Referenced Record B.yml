info:
  name: 26 Delete Referenced Record B
  type: http
  seq: 24

http:
  method: DELETE
  url: "{{baseUrl}}/api/record/{{labeledEntityIdB}}"
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
