info:
  name: 25 Next Ref Fills Gap
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}/api/records/nextref"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        // Gaps are filled — next ref is still 1 (lowest gap)
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Next ref is still 1 (lowest available gap)", function() {
          expect(res.body).to.equal(1);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
